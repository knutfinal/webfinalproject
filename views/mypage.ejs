<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>마이페이지 - 중고마켓 동네생활</title>

    <link href="/krds/css/token/krds_tokens.css" rel="stylesheet" />
    <link href="/krds/css/common/common.css" rel="stylesheet" />
    <link href="/krds/css/component/component.css" rel="stylesheet" />
    <script defer src="/krds/js/component/ui-script.js"></script>

    <style>
        body { background-color: #ffffff; } 
        #krds-footer { background-color: #f4f6f9; margin-top: 60px; padding: 30px 0; border-top: 1px solid #e9ecef; }
        
        .btn-logout-fix::before, .btn-logout-fix::after { display: none !important; }
        .btn-logout-fix { display: inline-flex !important; flex-direction: row !important; align-items: center !important; gap: 4px !important; width: auto !important; white-space: nowrap !important; }
        .btn-logout-fix svg { display: block !important; margin: 0 !important; }
        
        .header-branding { display: flex; align-items: center; width: 100%; }
        .header-branding .logo, .header-branding .logo a { background: none !important; background-image: none !important; margin: 0; display: flex; align-items: center; }

        .subpage-container { max-width: 1200px; margin: 60px auto; padding: 0 20px; min-height: 50vh; }
        .page-title { font-size: 2.8rem; font-weight: 700; color: #111; margin-bottom: 30px; border-bottom: 2px solid #222; padding-bottom: 16px; display: flex; justify-content: space-between; align-items: flex-end; }
        
        .quick-nav-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 20px; margin-bottom: 60px; }
        
        .nav-card { background: #fff; padding: 30px 20px; border-radius: 12px; border: 1px solid #ced4da; text-align: center; text-decoration: none; color: #333; transition: transform 0.2s, box-shadow 0.2s; box-shadow: 0 2px 8px rgba(0,0,0,0.05); }
        .nav-card:hover { transform: translateY(-5px); box-shadow: 0 8px 20px rgba(0,0,0,0.1); border-color: #246beb; color: #246beb; }
        .nav-card .icon { font-size: 3rem; margin-bottom: 15px; display: block; }
        .nav-card .title { font-size: 1.8rem; font-weight: 700; }

        .section-title { font-size: 2.2rem; font-weight: 700; color: #111; margin-bottom: 20px; }
        .item-grid { display: grid; grid-template-columns: repeat(3, 1fr); gap: 24px; }
        .item-card { background: #fff; border-radius: 12px; border: 1px solid #ced4da; overflow: hidden; box-shadow: 0 2px 8px rgba(0,0,0,0.05); transition: transform 0.2s; text-decoration: none; color: inherit; display: block; }
        .item-card:hover { transform: translateY(-5px); box-shadow: 0 8px 20px rgba(0,0,0,0.1); border-color: #246beb; }
        
        .item-img-placeholder { height: 200px; background-color: #f8f9fa; display: flex; align-items: center; justify-content: center; color: #adb5bd; font-size: 1.4rem; border-bottom: 1px solid #ced4da; }
        .item-img { width: 100%; height: 200px; object-fit: cover; border-bottom: 1px solid #ced4da; }
        
        .item-info { padding: 20px; }
        .item-info h4 { font-size: 1.8rem; font-weight: 600; color: #333; margin-bottom: 10px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
        .item-info .price { font-size: 1.6rem; font-weight: 700; color: #e03131; margin-bottom: 10px; }
        .item-info .seller { font-size: 1.3rem; color: #868e96; display: flex; justify-content: space-between; }
        .item-info .seller span.friend-badge { background: #e7f5ff; color: #1c7ed6; padding: 3px 8px; border-radius: 4px; font-size: 1.2rem; font-weight: 600; }
    
        .krds-main-menu .gnb-menu > li > .gnb-main-trigger::after { display: none !important; }
        </style>
</head>
<body>
<div id="wrap" class="g-wrap">

    <header id="krds-header">
        <div class="header-in">
            <div class="header-container">
                <div class="inner">
                    <div class="header-branding">
                        <h1 class="logo">
                            <a href="/" style="display: block;">
                                <img src="/sitelogo.png" alt="사이트 로고" style="height: 45px; width: auto; max-width: 100%; object-fit: contain;">
                            </a>
                        </h1>

                        <div class="header-actions" style="margin-left: auto;">
                            <% if (user) { %>
                                <span class="btn-navi my" style="background:none; cursor:default;">
                                    <strong><%= user.username %></strong>님
                                </span>
                                <a href="/logout" class="btn-navi btn-logout-fix" style="text-decoration: none; color: #333;">
                                    <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" style="margin-top:-2px;">
                                        <path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path>
                                        <polyline points="16 17 21 12 16 7"></polyline>
                                        <line x1="21" y1="12" x2="9" y2="12"></line>
                                    </svg>
                                    <span>로그아웃</span>
                                </a>
                            <% } else { %>
                                <a href="/login" class="btn-navi login">로그인</a>
                                <a href="/signup" class="btn-navi join">회원가입</a>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>

            <nav class="krds-main-menu">
                <div class="inner">
                    <ul class="gnb-menu" aria-label="메인 메뉴">
                        <li><button type="button" class="gnb-main-trigger" onclick="location.href='/info'">서비스 안내</button></li>
                        <li><button type="button" class="gnb-main-trigger" onclick="location.href='/board'">게시판</button></li>
                        <li><button type="button" class="gnb-main-trigger" onclick="location.href='/friends'">친구 관리</button></li>
                        <li><button type="button" class="gnb-main-trigger" onclick="location.href='/items'">판매 물품 정보</button></li>
                    </ul>
                </div>
            </nav>
        </div>
    </header>

    <div id="container" class="subpage-container">
        <h2 class="page-title">
            <span>나의 동네생활</span>
            <span style="font-size: 1.6rem; font-weight: 500; color: #666;"><%= user.username %>님, 환영합니다!</span>
        </h2>
        
        <div class="quick-nav-grid">
            <a href="/info" class="nav-card">
                <span class="icon">👤</span>
                <span class="title">서비스 안내</span>
            </a>
            <a href="/board" class="nav-card">
                <span class="icon">📝</span>
                <span class="title">게시판</span>
            </a>
            <a href="/friends" class="nav-card">
                <span class="icon">🤝</span>
                <span class="title">친구 관리</span>
            </a>
            <a href="/items" class="nav-card">
                <span class="icon">📦</span>
                <span class="title">판매 물품 정보</span>
            </a>
        </div>

        <h3 class="section-title">가장 최근 이웃이 올린 판매 물품</h3>
        <div class="item-grid">
            
            <% if (typeof recentItems !== 'undefined' && recentItems.length > 0) { %>
                <% recentItems.forEach(function(item) { %>
                <a href="/items" class="item-card">
                    <% if (item.image) { %>
                        <img src="<%= item.image %>" class="item-img" alt="물품 이미지">
                    <% } else { %>
                        <div class="item-img-placeholder">이미지 없음</div>
                    <% } %>
                    <div class="item-info">
                        <h4><%= item.title %></h4>
                        <p class="price"><%= item.price.toLocaleString() %>원</p>
                        <div class="seller">
                            <span>판매자: <%= item.author %></span>
                            <span class="friend-badge">내 이웃</span>
                        </div>
                    </div>
                </a>
                <% }) %>
            <% } else { %>
                <div style="grid-column: span 3; text-align: center; padding: 60px 0; background: #fff; border-radius: 12px; border: 1px dashed #ced4da; font-size: 1.6rem; color: #868e96;">
                    아직 이웃이 올린 판매 물품이 없습니다.<br>친구를 추가하고 새로운 소식을 확인해 보세요!
                </div>
            <% } %>
            </div>
    </div>

    <footer id="krds-footer">
        <div class="inner">
            <div class="f-btm">
                <div class="f-btm-text">
                    <div class="f-menu">
                        <a href="/terms" class="krds-btn small text">이용 안내</a>
                        <a href="/privacy" class="krds-btn small text">개인정보처리방침</a>
                        <a href="/copyright" class="krds-btn small text">저작권 정책</a>
                    </div>
                    <p class="f-copy">© 2026 Korea National University of Transportation. All rights reserved.</p>
                </div>
            </div>
        </div>
    </footer>

</div>
</body>
</html>