<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>판매 물품 정보 - 중고마켓 동네생활</title>
    <link href="/krds/css/token/krds_tokens.css" rel="stylesheet" />
    <link href="/krds/css/common/common.css" rel="stylesheet" />
    <link href="/krds/css/component/component.css" rel="stylesheet" />
    <script defer src="/krds/js/component/ui-script.js"></script>
    <style>
        body { background-color: #ffffff; } 
        #krds-footer { background-color: #f4f6f9; margin-top: 80px; padding: 30px 0; border-top: 1px solid #e9ecef; }
        .krds-main-menu .gnb-menu > li > .gnb-main-trigger::after { display: none !important; }
        .btn-logout-fix::before, .btn-logout-fix::after { display: none !important; }
        .btn-logout-fix { display: inline-flex !important; flex-direction: row !important; align-items: center !important; gap: 4px !important; width: auto !important; white-space: nowrap !important; }
        .btn-logout-fix svg { display: block !important; margin: 0 !important; }
        .header-branding { display: flex; align-items: center; width: 100%; }
        .header-branding .logo, .header-branding .logo a { background: none !important; background-image: none !important; margin: 0; display: flex; align-items: center; }
        .subpage-container { max-width: 1200px; margin: 60px auto; padding: 0 20px; min-height: 50vh; }
        .page-header { display: flex; justify-content: space-between; align-items: flex-end; margin-bottom: 30px; border-bottom: 2px solid #222; padding-bottom: 16px; }
        .page-title { font-size: 2.8rem; font-weight: 700; color: #111; margin: 0; }
        .btn-write, .btn-manage { background-color: #246beb; color: #fff; border: none; padding: 10px 24px; font-size: 1.6rem; font-weight: bold; border-radius: 6px; text-decoration: none; transition: 0.2s; }
        .btn-manage { background-color: #495057; }
        .item-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 24px; }
        .item-card { background: #fff; border-radius: 12px; border: 1px solid #ced4da; overflow: hidden; box-shadow: 0 2px 8px rgba(0,0,0,0.05); transition: transform 0.2s; cursor: pointer; text-decoration: none; color: inherit; display: block; position: relative; }
        .item-card:hover { transform: translateY(-5px); box-shadow: 0 8px 20px rgba(0,0,0,0.1); border-color: #246beb; }
        .item-img-placeholder { height: 200px; background-color: #f8f9fa; display: flex; align-items: center; justify-content: center; color: #adb5bd; font-size: 1.4rem; border-bottom: 1px solid #ced4da; }
        .item-img { width: 100%; height: 200px; object-fit: cover; border-bottom: 1px solid #ced4da; }
        .item-info { padding: 20px; }
        .item-info h4 { font-size: 1.8rem; font-weight: 600; color: #333; margin-bottom: 10px; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
        .item-info .price { font-size: 1.8rem; font-weight: 800; color: #111; margin-bottom: 10px; }
        .item-info .seller { font-size: 1.3rem; color: #868e96; display: flex; justify-content: space-between; }
        .my-items-section { margin-top: 80px; padding-top: 50px; border-top: 2px dashed #ced4da; }
        
        .status-badge { position: absolute; top: 10px; left: 10px; padding: 4px 10px; font-size: 1.2rem; font-weight: bold; border-radius: 4px; color: #fff; z-index: 10; }
        .status-selling { background-color: #246beb; }
        .status-requested { background-color: #f59f00; }
        .status-completed { background-color: #495057; }
    </style>
</head>
<body>
<div id="wrap" class="g-wrap">
    <header id="krds-header">
        <div class="header-in">
            <div class="header-container">
                <div class="inner">
                    <div class="header-branding">
                        <h1 class="logo"><a href="/" style="display: block;"><img src="/sitelogo.png" alt="사이트 로고" style="height: 45px; width: auto; max-width: 100%; object-fit: contain;"></a></h1>
                        <div class="header-actions" style="margin-left: auto;">
                            <% if (user) { %>
                                <span class="btn-navi my" style="background:none; cursor:default;"><strong><%= user.username %></strong>님</span>
                                <a href="/logout" class="btn-navi btn-logout-fix" style="text-decoration: none; color: #333;">로그아웃</a>
                            <% } else { %>
                                <a href="/login" class="btn-navi login">로그인</a>
                                <a href="/signup" class="btn-navi join">회원가입</a>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
            <nav class="krds-main-menu">
                <div class="inner">
                    <ul class="gnb-menu" aria-label="메인 메뉴">
                        <li><button type="button" class="gnb-main-trigger" onclick="location.href='/info'">서비스 안내</button></li>
                        <li><button type="button" class="gnb-main-trigger" onclick="location.href='/board'">게시판</button></li>
                        <li><button type="button" class="gnb-main-trigger" onclick="location.href='/friends'">친구 관리</button></li>
                        <li><button type="button" class="gnb-main-trigger" onclick="location.href='/items'">판매 물품 정보</button></li>
                    </ul>
                </div>
            </nav>
        </div>
    </header>

    <div id="container" class="subpage-container">
        <div class="page-header">
            <h2 class="page-title">동네 판매 물품 (전체)</h2>
            <a href="/items/write" class="btn-write">판매글 등록하기</a>
        </div>
        
        <div class="item-grid">
            <% items.forEach(function(item) { %>
            <a href="/items/detail/<%= item._id %>" class="item-card">
                <% if (item.status === 'selling') { %><div class="status-badge status-selling">판매중</div><% } %>
                <% if (item.status === 'requested') { %><div class="status-badge status-requested">예약중</div><% } %>
                <% if (item.status === 'completed') { %><div class="status-badge status-completed">판매완료</div><% } %>

                <% if (item.image) { %>
                    <img src="<%= item.image %>" class="item-img" alt="물품 이미지">
                <% } else { %>
                    <div class="item-img-placeholder">이미지 없음</div>
                <% } %>
                <div class="item-info">
                    <h4><%= item.title %></h4>
                    <p class="price"><%= item.price.toLocaleString() %>원</p>
                    <div class="seller"><span>판매자: <%= item.author %></span><span><%= item.createdAt.toLocaleDateString() %></span></div>
                </div>
            </a>
            <% }) %>
            <% if(items.length === 0) { %><p>아직 등록된 물품이 없습니다.</p><% } %>
        </div>

        <% if (user) { %>
        <div class="my-items-section">
            <div class="page-header">
                <h2 class="page-title">내가 등록한 물품</h2>
                <a href="/items/manage" class="btn-manage">내 물품 관리</a>
            </div>
            <div class="item-grid">
                <% myItems.forEach(function(item) { %>
                <a href="/items/detail/<%= item._id %>" class="item-card">
                    <% if (item.status === 'selling') { %><div class="status-badge status-selling">판매중</div><% } %>
                    <% if (item.status === 'requested') { %><div class="status-badge status-requested">요청받음</div><% } %>
                    <% if (item.status === 'completed') { %><div class="status-badge status-completed">판매완료</div><% } %>

                    <% if (item.image) { %>
                        <img src="<%= item.image %>" class="item-img" alt="물품 이미지">
                    <% } else { %>
                        <div class="item-img-placeholder">이미지 없음</div>
                    <% } %>
                    <div class="item-info">
                        <h4><%= item.title %></h4>
                        <p class="price"><%= item.price.toLocaleString() %>원</p>
                        <div class="seller">
                            <span style="color: #246beb; font-weight: bold;">판매자: <%= item.author %> (나)</span>
                            <span><%= item.createdAt.toLocaleDateString() %></span>
                        </div>
                    </div>
                </a>
                <% }) %>
                <% if(myItems.length === 0) { %><p>등록한 물품이 없습니다.</p><% } %>
            </div>
        </div>
        <% } %>
    </div>

    <footer id="krds-footer">
        <div class="inner">
            <div class="f-btm">
                <div class="f-btm-text">
                    <p class="f-copy">© 2026 Korea National University of Transportation. All rights reserved.</p>
                </div>
            </div>
        </div>
    </footer>
</div>
</body>
</html>